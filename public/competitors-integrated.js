(() => {
  'use strict';
  const root = window.Aliatek = window.Aliatek || {};
  const app = root.competitors = root.competitors || {};
  app.state = app.state || { days:30,busy:false,results:[],profile:null };
  const S=app.state,$=(s,p=document)=>p.querySelector(s),$$=(s,p=document)=>[...p.querySelectorAll(s)];
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const compact=n=>Intl.NumberFormat('en',{notation:'compact',maximumFractionDigits:1}).format(Number(n)||0);
  const money=n=>!n?'Undisclosed':n>=1e9?`$${(n/1e9).toFixed(1)}B`:n>=1e6?`$${(n/1e6).toFixed(1)}M`:`$${Math.round(n).toLocaleString()}`;

  async function json(url){const r=await fetch(url),d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Intelligence request failed.');return d}
  function status(text,bad=false){const el=$('#icStatus');if(!el)return;el.classList.toggle('bad',bad);el.querySelector('span').textContent=text}
  function bestLeader(profile){return profile?.leadership?.[0]||{name:'Not confidently identified',title:'Leadership not published',confidence:0,url:profile?.officialWebsite||''}}
  function numericFinancial(value){if(!value)return 0;const m=String(value).match(/[\d,.]+/);if(!m)return 0;let n=Number(m[0].replace(/,/g,'')),t=String(value).toLowerCase();if(t.includes('billion')||/\bbn\b/.test(t))n*=1e9;else if(t.includes('million')||/\bm\b/.test(t))n*=1e6;return n}
  function contracts(name,items=[]){return items.filter(x=>/contract|deal|award|agreement|partnership|procurement|grant|funding|selected by|collaboration/i.test(`${x.title} ${x.description}`)).slice(0,12).map(x=>({name,title:x.title,source:x.source,url:x.url}))}

  function fallbackProfile(name,news){
    const text=`${name} ${(news.items||[]).slice(0,180).map(x=>`${x.title} ${x.description}`).join(' ')}`.toLowerCase();
    const defs=[['education',['university','college','school','academy','student','faculty','campus']],['chronic_care',['chronic pain','chronic care','remote patient monitoring','home-based care']],['value_based_care',['value-based care','population health','care management']],['health_plan',['health plan','managed care','medicare advantage','medicaid']],['hospital_system',['hospital','medical center','health system']],['technology',['software','technology','artificial intelligence','cloud']],['banking',['bank','banking','credit union']],['retail',['retail','e-commerce']]];
    let sector='unknown',hits=0;for(const [s,keys] of defs){const h=keys.filter(k=>text.includes(k)).length;if(h>hits){sector=s;hits=h}}
    const peers={education:['Howard University','Spelman College','Clark Atlanta University','Hampton University','Tuskegee University'],chronic_care:['PonosCare','Omada Health','Virta Health','Teladoc Health','Hinge Health'],value_based_care:['Aledade','Cityblock Health','Oak Street Health','Signify Health','CareBridge'],health_plan:['UnitedHealth Group','Elevance Health','Cigna','Centene','Humana'],hospital_system:['HCA Healthcare','Mayo Clinic','Cleveland Clinic','CommonSpirit Health','Ascension'],technology:['Microsoft','Google','Amazon','Oracle','IBM']}[sector]||[];
    return{company:name,officialWebsite:'',websiteConfidence:0,industry:{sector,confidence:Math.min(70,hits*18),signals:[]},description:'Official website verification was unavailable, so Aliatek built a provisional profile from public evidence.',leadership:[],products:[],customers:[],partners:[],financials:{reportedRevenue:null,reportedProfit:null,status:'undisclosed'},competitors:peers.filter(x=>x.toLowerCase()!==name.toLowerCase()).map((n,i)=>({name:n,similarity:88-i*5,reason:'Public-evidence fallback peer'})),sources:[],provisional:true}
  }

  async function analyzeCompany(name,suppliedProfile=null){
    const news=await json(`/api/search?company=${encodeURIComponent(name)}&days=${S.days}&market=global`).catch(()=>({items:[],analytics:{totals:{mentions:0},percentages:{positive:0,negative:0},scores:{reputation:50}}}));
    let profile=suppliedProfile;
    if(!profile)profile=await json(`/api/company-profile?company=${encodeURIComponent(name)}`).catch(()=>fallbackProfile(name,news));
    const a=news.analytics||{},leader=bestLeader(profile);
    return{name,profile,leader,mentions:a?.totals?.mentions||0,sentiment:Math.max(0,Math.min(100,Math.round(50+(a?.percentages?.positive||0)*.6-(a?.percentages?.negative||0)*.7))),reputation:a?.scores?.reputation??50,revenue:numericFinancial(profile.financials?.reportedRevenue),profit:numericFinancial(profile.financials?.reportedProfit),contracts:contracts(name,news.items)}
  }

  function inject(){
    if($('#competitors'))return;const evidence=$('#evidence');if(!evidence)return;
    const nav=$('.sidebar nav');if(nav&&!nav.querySelector('[data-target="competitors"]')){const b=document.createElement('button');b.className='nav-btn';b.dataset.target='competitors';b.textContent='Competitors';nav.insertBefore(b,nav.querySelector('[data-target="evidence"]')||null)}
    const section=document.createElement('section');section.id='competitors';section.className='ic-section ic-reveal';section.innerHTML=`<div class="ic-orbit one"></div><div class="ic-orbit two"></div><div class="section-head"><div><small>COMPETITOR INTELLIGENCE</small><h3>Official-site research, leadership and verified market peers.</h3></div><button id="icScan" class="cta compact" type="button">Build comparison <span>→</span></button></div><div class="ic-controls"><label><span>⌕</span><input id="icCompany" placeholder="Uses your Command search, or enter another organization"></label><div class="ic-periods"><button data-days="7">7D</button><button class="active" data-days="30">30D</button><button data-days="90">90D</button></div></div><div id="icStatus" class="ic-status"><i></i><span>Run a company search to build its official website profile.</span></div><div id="icProfile" class="ic-profile"><div class="empty">No official website profile loaded.</div></div><div id="icSummary" class="ic-summary"></div><article class="panel ic-table-panel"><div class="panel-head"><div><small>ENTERPRISE COMPARISON</small><h3>Closest verified market peers</h3></div><span class="tag">WEBSITE-FIRST</span></div><div class="ic-table-wrap"><table><thead><tr><th>Organization</th><th>Leadership</th><th>Confidence</th><th>Sentiment</th><th>Mentions</th><th>Revenue</th><th>Profit</th><th>Contracts</th></tr></thead><tbody id="icRows"><tr><td colspan="8" class="empty">No comparison generated yet.</td></tr></tbody></table></div></article><article class="panel"><div class="panel-head"><div><small>CONTRACT INTELLIGENCE</small><h3>Deals, awards and partnerships</h3></div></div><div id="icContracts" class="ic-contracts"><div class="empty">No contract evidence loaded.</div></div></article>`;evidence.parentNode.insertBefore(section,evidence);
    $('#icScan').addEventListener('click',scan);$('#icCompany').addEventListener('keydown',e=>{if(e.key==='Enter')scan()});$$('.ic-periods button').forEach(b=>b.addEventListener('click',()=>{$$('.ic-periods button').forEach(x=>x.classList.remove('active'));b.classList.add('active');S.days=Number(b.dataset.days)}));
    const main=$('#companyInput');if(main)main.addEventListener('input',()=>{if(document.activeElement!==$('#icCompany'))$('#icCompany').value=main.value});
    $$('.sidebar .nav-btn').forEach(b=>b.addEventListener('click',()=>{const target=document.getElementById(b.dataset.target);if(!target)return;$$('.sidebar .nav-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');$('#pageTitle').textContent=b.textContent;target.scrollIntoView({behavior:'smooth',block:'start'})}));
    const ob=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('seen')),{threshold:.12});ob.observe(section);section.querySelectorAll('.panel').forEach(x=>ob.observe(x))
  }

  async function scan(){
    if(S.busy)return;const q=($('#icCompany')?.value||$('#companyInput')?.value||'').trim();if(q.length<2)return status('Enter an organization first.',true);
    S.busy=true;$('#icCompany').value=q;$('#icScan').disabled=true;status('Finding the official website and building a verified company profile…');
    try{
      let primaryNews=null,profile;
      try{profile=await json(`/api/company-profile?company=${encodeURIComponent(q)}`)}catch{primaryNews=await json(`/api/search?company=${encodeURIComponent(q)}&days=${S.days}&market=global`).catch(()=>({items:[],analytics:{totals:{mentions:0},percentages:{positive:0,negative:0},scores:{reputation:50}}}));profile=fallbackProfile(q,primaryNews);status('Official website verification was limited; continuing with a provisional public-evidence profile.')}
      S.profile=profile;const peers=(profile.competitors||[]).slice(0,5).map(x=>x.name),names=[q,...peers.filter(x=>x.toLowerCase()!==q.toLowerCase())];
      const settled=await Promise.allSettled(names.map((name,i)=>analyzeCompany(name,i===0?profile:null)));S.results=settled.filter(x=>x.status==='fulfilled').map(x=>x.value);if(!S.results.length)throw new Error('No usable company profiles were returned.');render()
    }catch(e){status(e.message,true)}finally{S.busy=false;$('#icScan').disabled=false}
  }

  function renderProfile(profile){const leader=bestLeader(profile),sector=profile.industry?.sector?.replaceAll('_',' ')||'Unknown',site=profile.officialWebsite?`<a href="${esc(profile.officialWebsite)}" target="_blank" rel="noopener">${esc(profile.officialWebsite)}</a>`:'<strong>Not verified</strong>';$('#icProfile').innerHTML=`<article class="ic-profile-card"><div><span>Verified website</span>${site}<small>${profile.provisional?'Provisional public-evidence profile':`${profile.websiteConfidence||0}% domain confidence`}</small></div><div><span>Classification</span><strong>${esc(sector.replace(/\b\w/g,c=>c.toUpperCase()))}</strong><small>${profile.industry?.confidence||0}% evidence confidence</small></div><div><span>Top leadership</span><strong>${esc(leader.name)}</strong><small>${esc(leader.title)} · ${leader.confidence||0}%</small></div><div><span>Official pages reviewed</span><strong>${profile.sources?.length||0}</strong><small>Website evidence sources</small></div></article><p class="ic-description">${esc(profile.description||'No official description was published.')}</p>`}
  function render(){const rows=S.results,primary=rows[0];renderProfile(primary.profile);const sector=primary.profile.industry?.sector?.replaceAll('_',' ')||'Unknown',verified=rows.filter(x=>x.leader.confidence>=70).length;status(primary.profile.provisional?`Compared ${rows.length} organizations; primary website verification remains provisional.`:`Compared ${rows.length} organizations using official-site profiles first.`);$('#icSummary').innerHTML=[['Competitive leader',[...rows].sort((a,b)=>b.reputation-a.reputation)[0]?.name||primary.name,`${Math.max(...rows.map(x=>x.reputation))}/100 reputation`],['Sub-industry',sector.replace(/\b\w/g,c=>c.toUpperCase()),`${primary.profile.industry?.confidence||0}% confidence`],['Leadership verified',verified,`of ${rows.length} organizations`]].map(x=>`<article><span>${esc(x[0])}</span><strong>${esc(x[1])}</strong><small>${esc(x[2])}</small></article>`).join('');$('#icRows').innerHTML=rows.map((x,i)=>`<tr><td>${i===0?'<span class="badge">Searched</span> ':''}${esc(x.name)}<small>${x.profile.officialWebsite?`<a href="${esc(x.profile.officialWebsite)}" target="_blank" rel="noopener">Official website</a>`:'Website unverified'}</small></td><td><b>${esc(x.leader.name)}</b><small>${esc(x.leader.title)}</small></td><td>${x.leader.confidence||0}%</td><td>${x.sentiment}/100</td><td>${compact(x.mentions)}</td><td>${money(x.revenue)}<small>${x.revenue?'reported signal':'private / undisclosed'}</small></td><td>${money(x.profit)}<small>${x.profit?'reported signal':'private / undisclosed'}</small></td><td>${x.contracts.length}</td></tr>`).join('');const all=rows.flatMap(x=>x.contracts);$('#icContracts').innerHTML=all.length?all.map(x=>`<article><div><b>${esc(x.name)}</b><small>${esc(x.source||'Public source')}</small></div><a href="${esc(x.url)}" target="_blank" rel="noopener">${esc(x.title)}</a></article>`).join(''):'<div class="empty">No contract-related evidence found in this period.</div>';$('#competitors').classList.add('data-ready')}

  app.scan=scan;app.render=render;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',inject);else inject();
})();
